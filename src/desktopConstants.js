/** Icon grid cell size on the desktop (matches DesktopCustomIcons). */
export const DESKTOP_ICON_WIDTH = 80
export const DESKTOP_ICON_HEIGHT = 96

/** Fixed menu bar height (see MenuBar.css). */
export const MENU_BAR_HEIGHT = 32

/** Minimum Y for desktop widgets and icons so they stay below the menu bar. */
export const DESKTOP_SAFE_TOP = MENU_BAR_HEIGHT + 8
